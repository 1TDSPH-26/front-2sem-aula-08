import { Link } from "react-router"

export default function() {
    return(
        <nav>
            <ul>
                <li><Link to="/" >Produtos</Link></li>
            </ul>
        </nav>
    )
}